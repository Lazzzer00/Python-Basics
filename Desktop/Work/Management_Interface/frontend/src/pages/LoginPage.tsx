import LoginComponent from "../components/LoginComponent";

export default function LoginPage() {
  return (
    <div className="flex justify-center items-center  lg:p-[4rem] md:p-[4rem] sm:p-0 bg-darkblack h-screen overflow-hidden">
      <LoginComponent />
    </div>
  );
}
